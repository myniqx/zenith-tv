import 'package:flutter/material.dart';
import '../../../core/app_theme.dart';

class ProfileManagerTv extends StatelessWidget {
  const ProfileManagerTv({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Profile Manager — coming soon',
          style: TextStyle(color: ZColors.mutedForeground)),
    );
  }
}
